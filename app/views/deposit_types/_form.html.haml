- form_for(@deposit_type, :html => { :class => "form" }) do |f|
  .group
    = f.label :name, nil, :class => "label"
    = f.text_field :name, :class => "text_field"

  - f.fields_for(:deposit_type_account_percentages) do |percentage_fields|
    .group
      - dtap = percentage_fields.object
      = percentage_fields.hidden_field :account_id, :value => dtap.account.id
      = percentage_fields.label :percentage, dtap.account.name, :class => "label"
      = percentage_fields.text_field :percentage, :class => "text_field percentage"
      \%

  .group.navform
    = submit_button_for(f)
    or 
    = link_to "back", :back
