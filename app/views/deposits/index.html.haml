%h2.title
  %span
    Total:
    = currency(@total)
  All Transactions

.inner
  %table.table
    %tr
      %th Date
      %th Amount
      %th User
      %th Accounts
      %th.last

    - @deposits.each do |deposit|
      %tr{ :class => cycle("odd", "") }
        %td= deposit.created_at.to_formatted_s(:long)
        %td= currency(deposit.allocated_dollars)
        %td= deposit.user
        %td= accounts(deposit)
        %td.last= link_to("Show", deposit)

